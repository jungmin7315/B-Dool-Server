
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-deployment
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: kang1521/notification:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085 # 컨테이너가 사용하는 포트를 8081로 설정
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  selector:
    app: notification-service
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80            # 외부에서 접근할 포트
      targetPort: 8085    # 컨테이너의 포트를 8081로 설정
